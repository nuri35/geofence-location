export enum ConfigNamespace {
  App = 'app',
  Database = 'database',
  Redis = 'redis',
}

export enum NodeEnvironment {
  Development = 'development',
  Test = 'test',
  Production = 'production',
}
